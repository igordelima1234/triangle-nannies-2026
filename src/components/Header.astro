---
import UtilityBar from './UtilityBar.astro';
import Nav from './Nav.astro';

interface Props {
	overlay?: boolean;
}

const { overlay = false } = Astro.props;
---

<UtilityBar />
<Nav overlay={overlay} />
